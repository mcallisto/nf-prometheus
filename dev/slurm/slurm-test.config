// Extra config to run the validation pipeline through the Slurm executor
// inside the dev/slurm container.
process {
    executor = 'slurm'
    queue = 'main'
    cpus = 1
}

prometheus {
    path = 'nf-prometheus-slurm.prom'
    pushgateway = 'http://pushgateway:9091'
}
